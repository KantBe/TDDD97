# TDDD97

## Run the server

`gunicorn -b 127.0.0.1:5000 --workers 1 --threads 100 server:app` in the main directory

Next, a login field will be promted. There is a gmail account that was created for testing this application.

Its credentials are:
username: `tddd97b11@gmail.com`
password: `H3vDCKvU`

Make sure to press `Continue` twice, as the app is not yet verified on Google.
